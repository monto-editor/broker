module Monto.Types where

import           Data.Text (Text)

type VersionID   = Int
type Source      = Text
type Language    = Text
type Product     = Text
type Port        = Int
type ServiceID   = Text
